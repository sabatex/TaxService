<Window x:Class="TaxService.Views.OrganizationListView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TaxService.Views"
        mc:Ignorable="d"
        Title="OrganizationListView" Height="450" Width="800" Loaded="Window_Loaded">
    <Window.CommandBindings>
        <CommandBinding Command="ApplicationCommands.New" Executed="CommandBinding_New"/>
        <CommandBinding Command="ApplicationCommands.Open" Executed="CommandBinding_Open" CanExecute="Open_CanExecute" />
    </Window.CommandBindings>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <ToolBar>
            <ToggleButton Command="ApplicationCommands.New" Content="{Binding RelativeSource={RelativeSource Self},Path=Command.Text}"/>
            <ToggleButton Command="ApplicationCommands.Open" Content="{Binding RelativeSource={RelativeSource Self},Path=Command.Text}"/>
        </ToolBar>
        <ListBox Grid.Row="1" ItemsSource="{Binding Path=Organizations}" Name="OrganizationsList" />
    </Grid>
</Window>
